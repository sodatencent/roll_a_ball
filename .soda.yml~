from: mac

scm:
  increment: true
  checkout: true

envs:
  version: 3.0.1

stages:
  build:
    # 向构建主机安装SODA提供的企业签名证书
    - pwd
    - ls
    - cat external/test.txt
    - cat external1/branch.txt
    - cat externalHttp/test.txt
    - plugin_entcert
    - plugin_unity3d --version 5.5.1f1
    - plugin_xcode
    - echo "test : test"
